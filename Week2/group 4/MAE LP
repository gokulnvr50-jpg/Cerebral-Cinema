{"nbformat":4,"nbformat_minor":0,"metadata":{"colab":{"provenance":[],"gpuType":"T4","authorship_tag":"ABX9TyOnNfzsI6TX4JASy49vAI45"},"kernelspec":{"name":"python3","display_name":"Python 3"},"language_info":{"name":"python"},"accelerator":"GPU"},"cells":[{"cell_type":"code","execution_count":1,"metadata":{"colab":{"base_uri":"https://localhost:8080/"},"id":"gDhDM9vs4zL5","executionInfo":{"status":"ok","timestamp":1781067741826,"user_tz":-330,"elapsed":302602,"user":{"displayName":"Gokul R","userId":"01606034480912303691"}},"outputId":"a73cbeec-3bb1-47f3-86ca-28d6fbc9b871"},"outputs":[{"output_type":"stream","name":"stdout","text":["Using device: cuda\n"]},{"output_type":"stream","name":"stderr","text":["100%|██████████| 170M/170M [00:04<00:00, 41.0MB/s]\n"]},{"output_type":"stream","name":"stdout","text":["\n","--- Starting MAE Pre-training ---\n","Pre-training Epoch [1/10] - MSE Loss: 0.9110\n","Pre-training Epoch [2/10] - MSE Loss: 0.7078\n","Pre-training Epoch [3/10] - MSE Loss: 0.6706\n","Pre-training Epoch [4/10] - MSE Loss: 0.6546\n","Pre-training Epoch [5/10] - MSE Loss: 0.6413\n","Pre-training Epoch [6/10] - MSE Loss: 0.6324\n","Pre-training Epoch [7/10] - MSE Loss: 0.6251\n","Pre-training Epoch [8/10] - MSE Loss: 0.6162\n","Pre-training Epoch [9/10] - MSE Loss: 0.6091\n","Pre-training Epoch [10/10] - MSE Loss: 0.6017\n","\n","--- Starting Linear Probing ---\n","Linear Probe Epoch [1/15] - Classification Loss: 1.8810\n","Linear Probe Epoch [2/15] - Classification Loss: 1.7818\n","Linear Probe Epoch [3/15] - Classification Loss: 1.7293\n","Linear Probe Epoch [4/15] - Classification Loss: 1.7168\n","Linear Probe Epoch [5/15] - Classification Loss: 1.6979\n","Linear Probe Epoch [6/15] - Classification Loss: 1.6852\n","Linear Probe Epoch [7/15] - Classification Loss: 1.6701\n","Linear Probe Epoch [8/15] - Classification Loss: 1.6587\n","Linear Probe Epoch [9/15] - Classification Loss: 1.6401\n","Linear Probe Epoch [10/15] - Classification Loss: 1.6290\n","Linear Probe Epoch [11/15] - Classification Loss: 1.6481\n","Linear Probe Epoch [12/15] - Classification Loss: 1.6479\n","Linear Probe Epoch [13/15] - Classification Loss: 1.6266\n","Linear Probe Epoch [14/15] - Classification Loss: 1.6390\n","Linear Probe Epoch [15/15] - Classification Loss: 1.6262\n","\n","--- Evaluating Model Accuracy ---\n","Final Linear Probe Accuracy on CIFAR-10: 42.78%\n"]}],"source":["import torch\n","import torch.nn as nn\n","import torch.optim as optim\n","import torchvision\n","import torchvision.transforms as transforms\n","from torch.utils.data import DataLoader\n","\n","# ==========================================\n","# 1. Data Preparation (CIFAR-10)\n","# ==========================================\n","def get_cifar10_loaders(batch_size=256):\n","    transform = transforms.Compose([\n","        transforms.ToTensor(),\n","        transforms.Normalize((0.4914, 0.4822, 0.4465), (0.2023, 0.1994, 0.2010))\n","    ])\n","\n","    # Download and load training and test sets\n","    train_set = torchvision.datasets.CIFAR10(root='./data', train=True, download=True, transform=transform)\n","    test_set = torchvision.datasets.CIFAR10(root='./data', train=False, download=True, transform=transform)\n","\n","    train_loader = DataLoader(train_set, batch_size=batch_size, shuffle=True, num_workers=2)\n","    test_loader = DataLoader(test_set, batch_size=batch_size, shuffle=False, num_workers=2)\n","\n","    return train_loader, test_loader\n","\n","# ==========================================\n","# 2. Model Architectures\n","# ==========================================\n","class MAE(nn.Module):\n","    \"\"\"\n","    Masked Autoencoder Structure.\n","    \"\"\"\n","    def __init__(self):\n","        super().__init__()\n","        # The Encoder extracts features from the image\n","        self.encoder = nn.Sequential(\n","            nn.Conv2d(3, 64, kernel_size=3, padding=1, stride=2),\n","            nn.ReLU(),\n","            nn.Conv2d(64, 128, kernel_size=3, padding=1, stride=2),\n","            nn.ReLU(),\n","            nn.AdaptiveAvgPool2d((1, 1)),\n","            nn.Flatten()\n","        )\n","        # The Decoder attempts to rebuild the original image\n","        self.decoder = nn.Sequential(\n","            nn.Linear(128, 512),\n","            nn.ReLU(),\n","            nn.Linear(512, 3 * 32 * 32)\n","        )\n","        self.embedding_dim = 128\n","\n","    def forward_encoder(self, x):\n","        # Extracts the latent representation\n","        return self.encoder(x)\n","\n","    def forward(self, x):\n","        # Full pass for pre-training reconstruction\n","        features = self.forward_encoder(x)\n","        reconstruction = self.decoder(features)\n","        return reconstruction.view(-1, 3, 32, 32)\n","\n","class LinearProbeClassifier(nn.Module):\n","    \"\"\"\n","    Attaches a single linear classification layer to the frozen MAE encoder.\n","    \"\"\"\n","    def __init__(self, encoder, num_classes=10):\n","        super().__init__()\n","        self.encoder = encoder\n","        self.fc = nn.Linear(encoder.embedding_dim, num_classes)\n","\n","        # CRITICAL: Freeze the encoder weights for linear probing\n","        for param in self.encoder.parameters():\n","            param.requires_grad = False\n","\n","    def forward(self, x):\n","        # Pass data through the frozen encoder without tracking gradients\n","        with torch.no_grad():\n","            features = self.encoder.forward_encoder(x)\n","        return self.fc(features)\n","\n","# ==========================================\n","# 3. Pre-training Phase (Self-Supervised)\n","# ==========================================\n","def pretrain_mae(model, train_loader, device, epochs=5):\n","    print(\"\\n--- Starting MAE Pre-training ---\")\n","    model.to(device)\n","    optimizer = optim.Adam(model.parameters(), lr=1e-3)\n","    criterion = nn.MSELoss() # Reconstruction loss\n","\n","    model.train()\n","    for epoch in range(epochs):\n","        total_loss = 0\n","        for images, _ in train_loader: # Labels are ignored during pre-training\n","            images = images.to(device)\n","\n","            optimizer.zero_grad()\n","            reconstructed = model(images)\n","\n","            loss = criterion(reconstructed, images)\n","            loss.backward()\n","            optimizer.step()\n","\n","            total_loss += loss.item()\n","\n","        avg_loss = total_loss / len(train_loader)\n","        print(f\"Pre-training Epoch [{epoch+1}/{epochs}] - MSE Loss: {avg_loss:.4f}\")\n","\n","# ==========================================\n","# 4. Linear Probing Phase (Evaluation)\n","# ==========================================\n","def linear_probe_evaluation(encoder, train_loader, test_loader, device, epochs=10):\n","    print(\"\\n--- Starting Linear Probing ---\")\n","    probe_model = LinearProbeClassifier(encoder, num_classes=10).to(device)\n","\n","    # Optimize ONLY the weights of the new linear layer\n","    optimizer = optim.SGD(probe_model.fc.parameters(), lr=0.1, momentum=0.9, weight_decay=1e-4)\n","    criterion = nn.CrossEntropyLoss()\n","\n","    # Train the linear probe\n","    probe_model.train()\n","    for epoch in range(epochs):\n","        total_loss = 0\n","        for images, labels in train_loader:\n","            images, labels = images.to(device), labels.to(device)\n","\n","            optimizer.zero_grad()\n","            outputs = probe_model(images)\n","            loss = criterion(outputs, labels)\n","            loss.backward()\n","            optimizer.step()\n","\n","            total_loss += loss.item()\n","\n","        avg_loss = total_loss / len(train_loader)\n","        print(f\"Linear Probe Epoch [{epoch+1}/{epochs}] - Classification Loss: {avg_loss:.4f}\")\n","\n","    # Evaluate the linear probe on test data\n","    print(\"\\n--- Evaluating Model Accuracy ---\")\n","    probe_model.eval()\n","    correct = 0\n","    total = 0\n","    with torch.no_grad():\n","        for images, labels in test_loader:\n","            images, labels = images.to(device), labels.to(device)\n","            outputs = probe_model(images)\n","\n","            _, predicted = torch.max(outputs.data, 1)\n","            total += labels.size(0)\n","            correct += (predicted == labels).sum().item()\n","\n","    accuracy = 100 * correct / total\n","    print(f\"Final Linear Probe Accuracy on CIFAR-10: {accuracy:.2f}%\")\n","    return accuracy\n","\n","# ==========================================\n","# Execution Block\n","# ==========================================\n","if __name__ == '__main__':\n","    # Automatically use GPU if available\n","    device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')\n","    print(f\"Using device: {device}\")\n","\n","    train_loader, test_loader = get_cifar10_loaders(batch_size=256)\n","\n","    # 1. Initialize and Pre-train MAE\n","    mae_model = MAE()\n","    pretrain_mae(mae_model, train_loader, device, epochs=10)\n","\n","    # 2. Evaluate using Linear Probe\n","    linear_probe_evaluation(mae_model, train_loader, test_loader, device, epochs=15)"]}]}
